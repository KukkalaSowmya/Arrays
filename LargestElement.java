public class LargestElement {
  public static void main(String[] args){
    int[] numbers = {2,4,-5,7,-9,3,-6,3,-8};
    int largest = numbers[0];
    for(int i=0; i<numbers.length;i++)
      if(numbers[i]largest)
        largest = numbers();
    System.out.println(largest);
  }
}